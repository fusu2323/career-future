---
phase: 03-job-profiling
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - JOB-01

must_haves:
  truths:
    - "Profile accuracy >= 90% verified via sampling"
    - "All automated tests pass"
    - "Phase 3 ready for Phase 4 (Neo4j graph) and Phase 8 (matching)"
  artifacts:
    - path: "data/processed/job_profiles.json"
      provides: ">= 10 job profiles, all 7 dimensions present"
  key_links:
    - from: "data/processed/job_profiles.json"
      to: "Phase 4 (Neo4j)"
      via: "JobProfile nodes already written in 03-02"
    - from: "data/processed/job_profiles.json"
      to: "Phase 8 (Matching)"
      via: "job profiles JSON consumed by matching engine"
---

<objective>
Verify job profiling output meets quality standards: profile accuracy >= 90%, all tests pass, outputs ready for downstream phases.
</objective>

<execution_context>
@C:/Users/Administrator/Desktop/职引未来/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@C:/Users/Administrator/Desktop/职引未来/.planning/phases/03-job-profiling/03-CONTEXT.md
@C:/Users/Administrator/Desktop/职引未来/.planning/phases/03-job-profiling/03-RESEARCH.md
@C:/Users/Administrator/Desktop/职引未来/.planning/phases/03-job-profiling/03-VALIDATION.md
@data/processed/job_profiles.json
@data/processed/jobs_cleaned.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual Accuracy Verification</name>
  <files>data/processed/job_profiles.json, data/processed/jobs_cleaned.json</files>
  <read_first>
data/processed/job_profiles.json
data/processed/jobs_cleaned.json
  </read_first>
  <action>
Perform manual sampling verification on generated profiles:

1. Open data/processed/job_profiles.json
2. For each of 3 randomly selected profiles:
   a. Note the job_type (e.g., "Java开发")
   b. Extract the core_skills list
   c. Search in data/processed/jobs_cleaned.json for records with matching 岗位名称
   d. Count how many of the LLM-suggested skills actually appear in the source job details
   e. Calculate: (skills found in source / total skills in profile) >= 80%
3. Check salary ranges:
   a. Note avg_salary_min and avg_salary_max from profile
   b. Calculate mean salary from source records
   c. Verify profile salary is within 20% of actual mean
4. Verify accuracy >= 90%:
   - 3 profiles sampled, each skill match >= 80%, salary within 20% = PASS
   - If any profile fails, document which one and why

WHAT-BUILT: Job profiling output: data/processed/job_profiles.json with >= 10 profiles, each containing all 7 dimensions

HOW-TO-VERIFY:
1. Open data/processed/job_profiles.json
2. For each of 3 randomly selected profiles:
   a. Note the job_type (e.g., "Java开发")
   b. Extract the core_skills list
   c. Search in data/processed/jobs_cleaned.json for records with matching 岗位名称
   d. Count how many of the LLM-suggested skills actually appear in the source job details
   e. Calculate: (skills found in source / total skills in profile) >= 80%
3. Check salary ranges:
   a. Note avg_salary_min and avg_salary_max from profile
   b. Calculate mean salary from source records
   c. Verify profile salary is within 20% of actual mean
4. Verify accuracy >= 90%

RESUME-SIGNAL: Type "approved" or describe issues requiring rework
  </action>
  <verify>
    <automated>Manual verification required - see acceptance criteria</automated>
  </verify>
  <acceptance_criteria>
- At least 3 profiles manually sampled
- Each sampled profile: >= 80% of listed skills appear in source job details
- Each sampled profile: salary range within 20% of actual mean
- Overall accuracy >= 90% (all sampled profiles pass)
  </acceptance_criteria>
  <done>Manual accuracy verification completed. Type "approved" to proceed or describe issues.</done>
</task>

<task type="auto">
  <name>Task 2: Automated Test Suite</name>
  <files>tests/phase03/test_profile_generation.py, tests/phase03/test_profile_quality.py</files>
  <read_first>
data/processed/job_profiles.json
  </read_first>
  <action>
Run the automated test suite:

```bash
cd "C:/Users/Administrator/Desktop/职引未来"
pytest tests/phase03/ -v --tb=short
```

This runs:
- test_profile_generation.py: profile count >= 10, all dimensions present, structure correct
- test_profile_quality.py: salary range, skills not empty, skill coverage, summary present
  </action>
  <verify>
    <automated>cd "C:/Users/Administrator/Desktop/职引未来" && python -m pytest tests/phase03/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
- pytest tests/phase03/ runs without errors
- Profile count >= 10
- All 7 dimensions present in all profiles
- test_profile_generation.py tests pass
- test_profile_quality.py tests pass
  </acceptance_criteria>
  <done>All tests in tests/phase03/ pass. Profile count >= 10. All 7 dimensions present in all profiles.</done>
</task>

<task type="auto">
  <name>Task 3: Downstream Readiness Check</name>
  <files>data/processed/job_profiles.json</files>
  <read_first>
data/processed/job_profiles.json
  </read_first>
  <action>
Verify job_profiles.json is ready for downstream phases:

1. Check Phase 4 (Neo4j) readiness:
   - Verify Neo4j nodes were written (if Neo4j was available)
   - If Neo4j write failed, note in summary that manual Neo4j population may be needed

2. Check Phase 8 (Matching) readiness:
   - Verify JSON structure matches what matching engine expects:
     - Each profile has: job_type, professional_skills, certificate_requirements, innovation_ability, learning_ability, stress_resistance, communication_ability, internship_importance, summary
   - Verify skill lists are arrays of strings (not empty)

3. Document output summary:
   - Number of profiles generated
   - Job types covered
   - Any profiles that had issues during generation
  </action>
  <verify>
    <automated>python -c "
import json
p = json.load(open('data/processed/job_profiles.json', encoding='utf-8'))
print(f'Profile count: {len(p)}')
for profile in p:
    dims = ['professional_skills', 'certificate_requirements', 'innovation_ability', 'learning_ability', 'stress_resistance', 'communication_ability', 'internship_importance']
    missing = [d for d in dims if d not in profile]
    if missing:
        print(f'FAIL: {profile.get(\"job_type\", \"unknown\")} missing {missing}')
    else:
        print(f'OK: {profile[\"job_type\"]}')
"</automated>
  </verify>
  <acceptance_criteria>
- Profile count >= 10
- All profiles have all 7 dimension keys
- All profiles have job_type string
- All profiles have summary string
  </acceptance_criteria>
  <done>Job profiles JSON verified complete with all 7 dimensions for all profiles. Phase 4 and Phase 8 readiness confirmed.</done>
</task>

</tasks>

<verification>
1. pytest tests/phase03/ -v --tb=short passes
2. Manual accuracy sampling: >= 90% of sampled profiles meet accuracy criteria
3. Downstream readiness verified
</verification>

<success_criteria>
- All automated tests pass (pytest tests/phase03/)
- Manual accuracy verification: >= 90% accuracy confirmed
- Data/processed/job_profiles.json ready for Phase 4 and Phase 8
- Phase 3 complete and documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-profiling/03-03-SUMMARY.md`
</output>
